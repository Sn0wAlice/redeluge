# Generated by tools/gen_openapi.py from contract/rpc-api.json.
# Do not edit by hand: run the generator instead, and the gate checks it.
openapi: 3.1.0

info:
  title: redeluge JSON-RPC API
  version: "1.8.1"
  summary: The HTTP API of the redeluge Web UI server.
  description:
    |-
      Everything a client does goes through one endpoint, `POST /json`, carrying
      JSON-RPC version 1. This is Deluge's own API, unchanged: redeluge reproduces
      it method for method so that clients written against the Python server keep
      working. The version above is the API's, not the fork's.

      A call is `{"method": ..., "params": [...], "id": ...}` and a reply is always
      HTTP 200 with `{"result": ..., "error": ..., "id": ...}`. A failure is
      reported in `error`, never in the status code, because that is what the
      shipped front end expects.

      Three methods answer before a session exists: `auth.login`,
      `auth.check_session` and `system.listMethods`. Everything else needs the
      session cookie that `auth.login` sets.

      Methods in the `core.*` and `daemon.*` namespaces are forwarded to the daemon
      over DelugeRPC; the rest are answered by the Web UI server itself.
  license:
    name: GPL-3.0-or-later
    url: https://www.gnu.org/licenses/gpl-3.0.html

servers:
  - url: http://localhost:8112
    description: The Web UI, as the container publishes it.

tags:
  - name: auth
    description: "Sessions and passwords. Answered by the Web UI server itself."
  - name: core
    description: "The daemon. Forwarded over DelugeRPC, unchanged."
  - name: daemon
    description: "The daemon itself rather than its torrents. Forwarded."
  - name: system
    description: "Introspection. Answered by the Web UI server itself, and not in the extracted contract because it carries no decorator."
  - name: web
    description: "The Web UI server: its own configuration, the connection to the daemon, and the aggregated update the interface polls."
  - name: webutils
    description: "Two methods Deluge exported under a second name. Aliases of their `web.*` twins."

security:
  - sessionCookie: []

paths:
  /json:
    post:
      summary: Call one method.
      description:
        |-
          The body names the method and its positional parameters. The response is
          HTTP 200 whatever happens; read `error` to find out whether the call worked.

          `auth.login` is the only method that mints a session: it sets the
          `_session_id` cookie, which every later call must carry.
      operationId: call
      tags: [auth, core, daemon, system, web, webutils]
      security:
        - sessionCookie: []
        - {}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Request"
      responses:
        "200":
          description: The call was dispatched. `error` says whether it worked.
          headers:
            Set-Cookie:
              description: Sent by `auth.login`, and refreshed on every call.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response"
        "400":
          description: The body was not a JSON-RPC call.

  /upload:
    post:
      summary: Stage one or more torrent files.
      description: |-
        The add-by-file dialog posts a multipart form here. The answer carries
        the paths the files were staged at, which then go to
        `web.get_torrent_info` and `web.add_torrents`.

        Always HTTP 200 with a `success` field: ExtJS's form submit treats any
        other status as a transport failure and shows its own message instead
        of this one. A file that does not parse as a torrent is refused, and
        one file may not exceed 10 MB.
      operationId: upload
      tags: [web]
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
      responses:
        "200":
          description: Whether the files were staged, and where.
          content:
            application/json:
              schema:
                type: object
                required: [success]
                properties:
                  success:
                    type: boolean
                  files:
                    type: array
                    description: Paths to pass to `web.get_torrent_info`.
                    items:
                      type: string
                  error:
                    type: string

  /:
    get:
      summary: The Web UI itself.
      description: The rendered page. Everything after it is a call to `/json`.
      operationId: index
      tags: [web]
      security:
        - {}
      responses:
        "200":
          description: The page.
          content:
            text/html:
              schema:
                type: string

  /render/{name}:
    get:
      summary: One rendered template fragment.
      description:
        |-
          The front end fetches a few fragments of markup rather than building them in
          JavaScript. Served from the templates compiled into the binary.
      operationId: render
      tags: [web]
      security:
        - {}
      parameters:
        - name: name
          in: path
          required: true
          description: The fragment's file name, such as `tab_status.html`.
          schema:
            type: string
      responses:
        "200":
          description: The fragment.
          content:
            text/html:
              schema:
                type: string
        "404":
          description: No such fragment.

  /flag/{code}:
    get:
      summary: The flag for a peer's country.
      description:
        |-
          The peers tab draws one of these per row. The code is ISO 3166-1 alpha-2 and
          is validated: anything else is a 404 rather than a path. Countries are only
          filled in when the daemon has a GeoIP database configured, so on a default
          install this is never asked for.
      operationId: flag
      tags: [web]
      security:
        - {}
      parameters:
        - name: code
          in: path
          required: true
          description: A two-letter country code, such as `fr`.
          schema:
            type: string
            pattern: "^[A-Za-z]{2}$"
      responses:
        "200":
          description: The flag.
          content:
            image/png:
              schema:
                type: string
                format: binary
        "404":
          description: No flag for that code.

  /{asset}:
    get:
      summary: A static asset.
      description:
        |-
          The ExtJS front end, its stylesheets, icons and images, compiled into the
          binary rather than read from a directory. A path that matches no asset is
          answered with the page, so a reload of a front-end route works.
      operationId: asset
      tags: [web]
      security:
        - {}
      parameters:
        - name: asset
          in: path
          required: true
          description: The asset path, such as `js/deluge-all.js`.
          schema:
            type: string
      responses:
        "200":
          description: The asset.

components:
  securitySchemes:
    sessionCookie:
      type: apiKey
      in: cookie
      name: _session_id
      description:
        |-
          Set by `auth.login`. The value is a session id with a four-digit checksum
          appended, which is the shape the Python server used; the checksum is not a
          security control, the id behind it is.

  schemas:
    RequestId:
      description: Echoed back on the response, so a client can pair them up.
      oneOf:
        - type: integer
        - type: string
        - type: "null"

    Error:
      type: object
      description: Present on `error` when a call failed, and `null` otherwise.
      required: [message, code]
      properties:
        message:
          type: string
          description: What went wrong, in words.
        code:
          type: integer
          description:
            |-
              1 for a call that needed a session and did not have one, 2 for a method this
              server does not know, 3 for a failure inside the method, 4 for an error the
              daemon reported.

    Response:
      type: object
      description: Always HTTP 200. Exactly one of `result` and `error` is set.
      required: [result, error, id]
      properties:
        result:
          description: Whatever the method returns. `null` when it failed.
        error:
          oneOf:
            - $ref: "#/components/schemas/Error"
            - type: "null"
        id:
          $ref: "#/components/schemas/RequestId"

    Request:
      description: One call. The `method` field picks which shape applies.
      discriminator:
        propertyName: method
      oneOf:
        - $ref: "#/components/schemas/AuthChangePassword"
        - $ref: "#/components/schemas/AuthCheckSession"
        - $ref: "#/components/schemas/AuthDeleteSession"
        - $ref: "#/components/schemas/AuthLogin"
        - $ref: "#/components/schemas/CoreAddTorrentFile"
        - $ref: "#/components/schemas/CoreAddTorrentFileAsync"
        - $ref: "#/components/schemas/CoreAddTorrentFiles"
        - $ref: "#/components/schemas/CoreAddTorrentMagnet"
        - $ref: "#/components/schemas/CoreAddTorrentUrl"
        - $ref: "#/components/schemas/CoreConnectPeer"
        - $ref: "#/components/schemas/CoreCreateAccount"
        - $ref: "#/components/schemas/CoreCreateTorrent"
        - $ref: "#/components/schemas/CoreForceReannounce"
        - $ref: "#/components/schemas/CoreForceRecheck"
        - $ref: "#/components/schemas/CoreGetAuthLevelsMappings"
        - $ref: "#/components/schemas/CoreGetCompletionPaths"
        - $ref: "#/components/schemas/CoreGetConfig"
        - $ref: "#/components/schemas/CoreGetConfigValue"
        - $ref: "#/components/schemas/CoreGetConfigValues"
        - $ref: "#/components/schemas/CoreGetExternalIp"
        - $ref: "#/components/schemas/CoreGetFilterTree"
        - $ref: "#/components/schemas/CoreGetFreeSpace"
        - $ref: "#/components/schemas/CoreGetKnownAccounts"
        - $ref: "#/components/schemas/CoreGetLibtorrentVersion"
        - $ref: "#/components/schemas/CoreGetListenPort"
        - $ref: "#/components/schemas/CoreGetMagnetUri"
        - $ref: "#/components/schemas/CoreGetPathSize"
        - $ref: "#/components/schemas/CoreGetProxy"
        - $ref: "#/components/schemas/CoreGetSessionState"
        - $ref: "#/components/schemas/CoreGetSessionStatus"
        - $ref: "#/components/schemas/CoreGetSslListenPort"
        - $ref: "#/components/schemas/CoreGetTorrentStatus"
        - $ref: "#/components/schemas/CoreGetTorrentsStatus"
        - $ref: "#/components/schemas/CoreGlob"
        - $ref: "#/components/schemas/CoreIsSessionPaused"
        - $ref: "#/components/schemas/CoreMoveStorage"
        - $ref: "#/components/schemas/CorePauseSession"
        - $ref: "#/components/schemas/CorePauseTorrent"
        - $ref: "#/components/schemas/CorePauseTorrents"
        - $ref: "#/components/schemas/CorePrefetchMagnetMetadata"
        - $ref: "#/components/schemas/CoreQueueBottom"
        - $ref: "#/components/schemas/CoreQueueDown"
        - $ref: "#/components/schemas/CoreQueueTop"
        - $ref: "#/components/schemas/CoreQueueUp"
        - $ref: "#/components/schemas/CoreRemoveAccount"
        - $ref: "#/components/schemas/CoreRemoveTorrent"
        - $ref: "#/components/schemas/CoreRemoveTorrents"
        - $ref: "#/components/schemas/CoreRenameFiles"
        - $ref: "#/components/schemas/CoreRenameFolder"
        - $ref: "#/components/schemas/CoreResumeSession"
        - $ref: "#/components/schemas/CoreResumeTorrent"
        - $ref: "#/components/schemas/CoreResumeTorrents"
        - $ref: "#/components/schemas/CoreSetConfig"
        - $ref: "#/components/schemas/CoreSetSslTorrentCert"
        - $ref: "#/components/schemas/CoreSetTorrentAutoManaged"
        - $ref: "#/components/schemas/CoreSetTorrentFilePriorities"
        - $ref: "#/components/schemas/CoreSetTorrentMaxConnections"
        - $ref: "#/components/schemas/CoreSetTorrentMaxDownloadSpeed"
        - $ref: "#/components/schemas/CoreSetTorrentMaxUploadSlots"
        - $ref: "#/components/schemas/CoreSetTorrentMaxUploadSpeed"
        - $ref: "#/components/schemas/CoreSetTorrentMoveCompleted"
        - $ref: "#/components/schemas/CoreSetTorrentMoveCompletedPath"
        - $ref: "#/components/schemas/CoreSetTorrentOptions"
        - $ref: "#/components/schemas/CoreSetTorrentPrioritizeFirstLast"
        - $ref: "#/components/schemas/CoreSetTorrentRemoveAtRatio"
        - $ref: "#/components/schemas/CoreSetTorrentStopAtRatio"
        - $ref: "#/components/schemas/CoreSetTorrentStopRatio"
        - $ref: "#/components/schemas/CoreSetTorrentTrackers"
        - $ref: "#/components/schemas/CoreTestListenPort"
        - $ref: "#/components/schemas/CoreUpdateAccount"
        - $ref: "#/components/schemas/DaemonAuthorizedCall"
        - $ref: "#/components/schemas/DaemonGetMethodList"
        - $ref: "#/components/schemas/DaemonGetVersion"
        - $ref: "#/components/schemas/DaemonShutdown"
        - $ref: "#/components/schemas/SystemListMethods"
        - $ref: "#/components/schemas/WebAddHost"
        - $ref: "#/components/schemas/WebAddTorrents"
        - $ref: "#/components/schemas/WebConnect"
        - $ref: "#/components/schemas/WebConnected"
        - $ref: "#/components/schemas/WebDeregisterEventListener"
        - $ref: "#/components/schemas/WebDisconnect"
        - $ref: "#/components/schemas/WebDownloadTorrentFromUrl"
        - $ref: "#/components/schemas/WebEditHost"
        - $ref: "#/components/schemas/WebGetConfig"
        - $ref: "#/components/schemas/WebGetEvents"
        - $ref: "#/components/schemas/WebGetHostStatus"
        - $ref: "#/components/schemas/WebGetHosts"
        - $ref: "#/components/schemas/WebGetMagnetInfo"
        - $ref: "#/components/schemas/WebGetTorrentFiles"
        - $ref: "#/components/schemas/WebGetTorrentInfo"
        - $ref: "#/components/schemas/WebGetTorrentStatus"
        - $ref: "#/components/schemas/WebRegisterEventListener"
        - $ref: "#/components/schemas/WebRemoveHost"
        - $ref: "#/components/schemas/WebSetConfig"
        - $ref: "#/components/schemas/WebSetTheme"
        - $ref: "#/components/schemas/WebStartDaemon"
        - $ref: "#/components/schemas/WebStopDaemon"
        - $ref: "#/components/schemas/WebUpdateUi"
        - $ref: "#/components/schemas/WebutilsGetLanguages"
        - $ref: "#/components/schemas/WebutilsGetThemes"

    AuthChangePassword:
      type: object
      description:
        |-
          Change the password.
          Authorisation: normal.
          Answered by the Web UI server.
          Required parameters, in order: `old_password`, `new_password`.
          Defined in the Python implementation at `deluge/ui/web/auth.py:205`.
      required: [method]
      properties:
        method:
          type: string
          const: "auth.change_password"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 2
          maxItems: 2
          prefixItems:
            - description: "old_password"
            - description: "new_password"

    AuthCheckSession:
      type: object
      description:
        |-
          Check a session to see if it's still valid.
          Authorisation: none, answered before a session exists.
          Answered by the Web UI server.
          Optional, in order after those: `session_id` (default `None`).
          Defined in the Python implementation at `deluge/ui/web/auth.py:219`.
      required: [method]
      properties:
        method:
          type: string
          const: "auth.check_session"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 0
          maxItems: 1
          prefixItems:
            - description: "session_id — optional, defaults to None"

    AuthDeleteSession:
      type: object
      description:
        |-
          Removes a session.
          Authorisation: normal.
          Answered by the Web UI server.
          Takes no parameters.
          Defined in the Python implementation at `deluge/ui/web/auth.py:229`.
      required: [method]
      properties:
        method:
          type: string
          const: "auth.delete_session"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Empty; this method takes no parameters.
          maxItems: 0
          default: []

    AuthLogin:
      type: object
      description:
        |-
          Test a password to see if it's valid.
          Authorisation: none, answered before a session exists.
          Answered by the Web UI server.
          Required parameters, in order: `password`.
          Defined in the Python implementation at `deluge/ui/web/auth.py:240`.
      required: [method]
      properties:
        method:
          type: string
          const: "auth.login"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 1
          maxItems: 1
          prefixItems:
            - description: "password"

    CoreAddTorrentFile:
      type: object
      description:
        |-
          Adds a torrent file to the session.
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Required parameters, in order: `filename`, `filedump`, `options`.
          Defined in the Python implementation at `deluge/core/core.py:457`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.add_torrent_file"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 3
          maxItems: 3
          prefixItems:
            - description: "filename (str)"
            - description: "filedump (Union[str, bytes])"
            - description: "options (dict)"

    CoreAddTorrentFileAsync:
      type: object
      description:
        |-
          Adds a torrent file to the session asynchronously.
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Required parameters, in order: `filename`, `filedump`, `options`.
          Optional, in order after those: `save_state` (default `True`).
          Defined in the Python implementation at `deluge/core/core.py:403`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.add_torrent_file_async"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 3
          maxItems: 4
          prefixItems:
            - description: "filename (str)"
            - description: "filedump (str)"
            - description: "options (dict)"
            - description: "save_state (bool) — optional, defaults to True"

    CoreAddTorrentFiles:
      type: object
      description:
        |-
          Adds multiple torrent files to the session asynchronously.
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Required parameters, in order: `torrent_files`.
          Defined in the Python implementation at `deluge/core/core.py:484`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.add_torrent_files"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 1
          maxItems: 1
          prefixItems:
            - description: "torrent_files (List[Tuple[str, Union[str, bytes], dict]])"

    CoreAddTorrentMagnet:
      type: object
      description:
        |-
          Adds a torrent from a magnet link.
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Required parameters, in order: `uri`, `options`.
          Defined in the Python implementation at `deluge/core/core.py:551`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.add_torrent_magnet"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 2
          maxItems: 2
          prefixItems:
            - description: "uri (str)"
            - description: "options (dict)"

    CoreAddTorrentUrl:
      type: object
      description:
        |-
          Adds a torrent from a URL. Deluge will attempt to fetch the torrent
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Required parameters, in order: `url`, `options`.
          Optional, in order after those: `headers` (default `None`).
          Defined in the Python implementation at `deluge/core/core.py:515`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.add_torrent_url"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 2
          maxItems: 3
          prefixItems:
            - description: "url (str)"
            - description: "options (dict)"
            - description: "headers (dict) — optional, defaults to None"

    CoreConnectPeer:
      type: object
      description:
        |-
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Required parameters, in order: `torrent_id`, `ip`, `port`.
          Defined in the Python implementation at `deluge/core/core.py:674`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.connect_peer"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 3
          maxItems: 3
          prefixItems:
            - description: "torrent_id (str)"
            - description: "ip (str)"
            - description: "port (int)"

    CoreCreateAccount:
      type: object
      description:
        |-
          Authorisation: admin.
          Forwarded to the daemon over DelugeRPC.
          Required parameters, in order: `username`, `password`, `authlevel`.
          Defined in the Python implementation at `deluge/core/core.py:1361`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.create_account"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 3
          maxItems: 3
          prefixItems:
            - description: "username (str)"
            - description: "password (str)"
            - description: "authlevel (str)"

    CoreCreateTorrent:
      type: object
      description:
        |-
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Required parameters, in order: `path`, `tracker`, `piece_length`.
          Optional, in order after those: `comment` (default `None`), `target` (default `None`), `webseeds` (default `None`), `private` (default `False`), `created_by` (default `None`), `trackers` (default `None`), `add_to_session` (default `False`), `torrent_format` (default `metafile.TorrentFormat.V1`), `ca_cert` (default `None`).
          Defined in the Python implementation at `deluge/core/core.py:1053`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.create_torrent"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 3
          maxItems: 12
          prefixItems:
            - description: "path"
            - description: "tracker"
            - description: "piece_length"
            - description: "comment — optional, defaults to None"
            - description: "target — optional, defaults to None"
            - description: "webseeds — optional, defaults to None"
            - description: "private — optional, defaults to False"
            - description: "created_by — optional, defaults to None"
            - description: "trackers — optional, defaults to None"
            - description: "add_to_session — optional, defaults to False"
            - description: "torrent_format — optional, defaults to metafile.TorrentFormat.V1"
            - description: "ca_cert — optional, defaults to None"

    CoreForceReannounce:
      type: object
      description:
        |-
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Required parameters, in order: `torrent_ids`.
          Defined in the Python implementation at `deluge/core/core.py:651`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.force_reannounce"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 1
          maxItems: 1
          prefixItems:
            - description: "torrent_ids (List[str])"

    CoreForceRecheck:
      type: object
      description:
        |-
          Forces a data recheck on torrent_ids
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Required parameters, in order: `torrent_ids`.
          Defined in the Python implementation at `deluge/core/core.py:938`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.force_recheck"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 1
          maxItems: 1
          prefixItems:
            - description: "torrent_ids (List[str])"

    CoreGetAuthLevelsMappings:
      type: object
      description:
        |-
          Authorisation: none, answered before a session exists.
          Forwarded to the daemon over DelugeRPC.
          Takes no parameters.
          Defined in the Python implementation at `deluge/core/core.py:1357`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.get_auth_levels_mappings"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Empty; this method takes no parameters.
          maxItems: 0
          default: []

    CoreGetCompletionPaths:
      type: object
      description:
        |-
          Returns the available path completions for the input value.
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Required parameters, in order: `args`.
          Defined in the Python implementation at `deluge/core/core.py:1348`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.get_completion_paths"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 1
          maxItems: 1
          prefixItems:
            - description: "args (Dict[str, Any])"

    CoreGetConfig:
      type: object
      description:
        |-
          Get all the preferences as a dictionary
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Takes no parameters.
          Defined in the Python implementation at `deluge/core/core.py:849`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.get_config"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Empty; this method takes no parameters.
          maxItems: 0
          default: []

    CoreGetConfigValue:
      type: object
      description:
        |-
          Get the config value for key
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Required parameters, in order: `key`.
          Defined in the Python implementation at `deluge/core/core.py:854`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.get_config_value"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 1
          maxItems: 1
          prefixItems:
            - description: "key (str)"

    CoreGetConfigValues:
      type: object
      description:
        |-
          Get the config values for the entered keys
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Required parameters, in order: `keys`.
          Defined in the Python implementation at `deluge/core/core.py:859`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.get_config_values"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 1
          maxItems: 1
          prefixItems:
            - description: "keys (List[str])"

    CoreGetExternalIp:
      type: object
      description:
        |-
          Returns the external IP address received from libtorrent.
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Takes no parameters.
          Defined in the Python implementation at `deluge/core/core.py:1334`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.get_external_ip"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Empty; this method takes no parameters.
          maxItems: 0
          default: []

    CoreGetFilterTree:
      type: object
      description:
        |-
          returns {field: [(value,count)] }
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Optional, in order after those: `show_zero_hits` (default `True`), `hide_cat` (default `None`).
          Defined in the Python implementation at `deluge/core/core.py:834`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.get_filter_tree"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 0
          maxItems: 2
          prefixItems:
            - description: "show_zero_hits (bool) — optional, defaults to True"
            - description: "hide_cat (List[str]) — optional, defaults to None"

    CoreGetFreeSpace:
      type: object
      description:
        |-
          Returns the number of free bytes at path
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Optional, in order after those: `path` (default `None`).
          Defined in the Python implementation at `deluge/core/core.py:1311`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.get_free_space"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 0
          maxItems: 1
          prefixItems:
            - description: "path (str) — optional, defaults to None"

    CoreGetKnownAccounts:
      type: object
      description:
        |-
          Authorisation: admin.
          Forwarded to the daemon over DelugeRPC.
          Takes no parameters.
          Defined in the Python implementation at `deluge/core/core.py:1353`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.get_known_accounts"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Empty; this method takes no parameters.
          maxItems: 0
          default: []

    CoreGetLibtorrentVersion:
      type: object
      description:
        |-
          Returns the libtorrent version.
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Takes no parameters.
          Defined in the Python implementation at `deluge/core/core.py:1339`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.get_libtorrent_version"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Empty; this method takes no parameters.
          maxItems: 0
          default: []

    CoreGetListenPort:
      type: object
      description:
        |-
          Returns the active listen port
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Takes no parameters.
          Defined in the Python implementation at `deluge/core/core.py:873`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.get_listen_port"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Empty; this method takes no parameters.
          maxItems: 0
          default: []

    CoreGetMagnetUri:
      type: object
      description:
        |-
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Required parameters, in order: `torrent_id`.
          Defined in the Python implementation at `deluge/core/core.py:971`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.get_magnet_uri"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 1
          maxItems: 1
          prefixItems:
            - description: "torrent_id (str)"

    CoreGetPathSize:
      type: object
      description:
        |-
          Returns the size of the file or folder 'path' and -1 if the path is
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Required parameters, in order: `path`.
          Defined in the Python implementation at `deluge/core/core.py:1047`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.get_path_size"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 1
          maxItems: 1
          prefixItems:
            - description: "path"

    CoreGetProxy:
      type: object
      description:
        |-
          Returns the proxy settings
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Takes no parameters.
          Defined in the Python implementation at `deluge/core/core.py:889`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.get_proxy"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Empty; this method takes no parameters.
          maxItems: 0
          default: []

    CoreGetSessionState:
      type: object
      description:
        |-
          Returns a list of torrent_ids in the session.
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Takes no parameters.
          Defined in the Python implementation at `deluge/core/core.py:843`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.get_session_state"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Empty; this method takes no parameters.
          maxItems: 0
          default: []

    CoreGetSessionStatus:
      type: object
      description:
        |-
          Gets the session status values for 'keys', these keys are taking
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Required parameters, in order: `keys`.
          Defined in the Python implementation at `deluge/core/core.py:620`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.get_session_status"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 1
          maxItems: 1
          prefixItems:
            - description: "keys (List[str])"

    CoreGetSslListenPort:
      type: object
      description:
        |-
          Returns the active SSL listen port
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Takes no parameters.
          Defined in the Python implementation at `deluge/core/core.py:878`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.get_ssl_listen_port"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Empty; this method takes no parameters.
          maxItems: 0
          default: []

    CoreGetTorrentStatus:
      type: object
      description:
        |-
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Required parameters, in order: `torrent_id`, `keys`.
          Optional, in order after those: `diff` (default `False`).
          Defined in the Python implementation at `deluge/core/core.py:801`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.get_torrent_status"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 2
          maxItems: 3
          prefixItems:
            - description: "torrent_id (str)"
            - description: "keys (List[str])"
            - description: "diff (bool) — optional, defaults to False"

    CoreGetTorrentsStatus:
      type: object
      description:
        |-
          returns all torrents , optionally filtered by filter_dict.
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Required parameters, in order: `filter_dict`, `keys`.
          Optional, in order after those: `diff` (default `False`).
          Defined in the Python implementation at `deluge/core/core.py:818`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.get_torrents_status"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 2
          maxItems: 3
          prefixItems:
            - description: "filter_dict (dict)"
            - description: "keys (List[str])"
            - description: "diff (bool) — optional, defaults to False"

    CoreGlob:
      type: object
      description:
        |-
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Required parameters, in order: `path`.
          Defined in the Python implementation at `deluge/core/core.py:1284`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.glob"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 1
          maxItems: 1
          prefixItems:
            - description: "path (str)"

    CoreIsSessionPaused:
      type: object
      description:
        |-
          Returns the activity of the session
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Takes no parameters.
          Defined in the Python implementation at `deluge/core/core.py:754`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.is_session_paused"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Empty; this method takes no parameters.
          maxItems: 0
          default: []

    CoreMoveStorage:
      type: object
      description:
        |-
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Required parameters, in order: `torrent_ids`, `dest`.
          Defined in the Python implementation at `deluge/core/core.py:731`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.move_storage"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 2
          maxItems: 2
          prefixItems:
            - description: "torrent_ids (List[str])"
            - description: "dest (str)"

    CorePauseSession:
      type: object
      description:
        |-
          Pause the entire session
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Takes no parameters.
          Defined in the Python implementation at `deluge/core/core.py:738`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.pause_session"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Empty; this method takes no parameters.
          maxItems: 0
          default: []

    CorePauseTorrent:
      type: object
      description:
        |-
          Pauses a torrent
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Required parameters, in order: `torrent_id`.
          Defined in the Python implementation at `deluge/core/core.py:657`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.pause_torrent"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 1
          maxItems: 1
          prefixItems:
            - description: "torrent_id (str)"

    CorePauseTorrents:
      type: object
      description:
        |-
          Pauses a list of torrents
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Optional, in order after those: `torrent_ids` (default `None`).
          Defined in the Python implementation at `deluge/core/core.py:666`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.pause_torrents"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 0
          maxItems: 1
          prefixItems:
            - description: "torrent_ids (List[str]) — optional, defaults to None"

    CorePrefetchMagnetMetadata:
      type: object
      description:
        |-
          Download magnet metadata without adding to Deluge session.
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Required parameters, in order: `magnet`.
          Optional, in order after those: `timeout` (default `30`).
          Defined in the Python implementation at `deluge/core/core.py:437`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.prefetch_magnet_metadata"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 1
          maxItems: 2
          prefixItems:
            - description: "magnet (str)"
            - description: "timeout (int) — optional, defaults to 30"

    CoreQueueBottom:
      type: object
      description:
        |-
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Required parameters, in order: `torrent_ids`.
          Defined in the Python implementation at `deluge/core/core.py:1270`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.queue_bottom"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 1
          maxItems: 1
          prefixItems:
            - description: "torrent_ids (List[str])"

    CoreQueueDown:
      type: object
      description:
        |-
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Required parameters, in order: `torrent_ids`.
          Defined in the Python implementation at `deluge/core/core.py:1245`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.queue_down"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 1
          maxItems: 1
          prefixItems:
            - description: "torrent_ids (List[str])"

    CoreQueueTop:
      type: object
      description:
        |-
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Required parameters, in order: `torrent_ids`.
          Defined in the Python implementation at `deluge/core/core.py:1206`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.queue_top"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 1
          maxItems: 1
          prefixItems:
            - description: "torrent_ids (List[str])"

    CoreQueueUp:
      type: object
      description:
        |-
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Required parameters, in order: `torrent_ids`.
          Defined in the Python implementation at `deluge/core/core.py:1220`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.queue_up"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 1
          maxItems: 1
          prefixItems:
            - description: "torrent_ids (List[str])"

    CoreRemoveAccount:
      type: object
      description:
        |-
          Authorisation: admin.
          Forwarded to the daemon over DelugeRPC.
          Required parameters, in order: `username`.
          Defined in the Python implementation at `deluge/core/core.py:1369`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.remove_account"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 1
          maxItems: 1
          prefixItems:
            - description: "username (str)"

    CoreRemoveTorrent:
      type: object
      description:
        |-
          Removes a single torrent from the session.
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Required parameters, in order: `torrent_id`, `remove_data`.
          Defined in the Python implementation at `deluge/core/core.py:566`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.remove_torrent"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 2
          maxItems: 2
          prefixItems:
            - description: "torrent_id (str)"
            - description: "remove_data (bool)"

    CoreRemoveTorrents:
      type: object
      description:
        |-
          Remove multiple torrents from the session.
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Required parameters, in order: `torrent_ids`, `remove_data`.
          Defined in the Python implementation at `deluge/core/core.py:583`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.remove_torrents"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 2
          maxItems: 2
          prefixItems:
            - description: "torrent_ids (List[str])"
            - description: "remove_data (bool)"

    CoreRenameFiles:
      type: object
      description:
        |-
          Rename files in ``torrent_id``.  Since this is an asynchronous operation by
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Required parameters, in order: `torrent_id`, `filenames`.
          Defined in the Python implementation at `deluge/core/core.py:1162`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.rename_files"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 2
          maxItems: 2
          prefixItems:
            - description: "torrent_id (str)"
            - description: "filenames (List[Tuple[int, str]])"

    CoreRenameFolder:
      type: object
      description:
        |-
          Renames the 'folder' to 'new_folder' in 'torrent_id'.  Watch for the
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Required parameters, in order: `torrent_id`, `folder`, `new_folder`.
          Defined in the Python implementation at `deluge/core/core.py:1185`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.rename_folder"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 3
          maxItems: 3
          prefixItems:
            - description: "torrent_id (str)"
            - description: "folder (str)"
            - description: "new_folder (str)"

    CoreResumeSession:
      type: object
      description:
        |-
          Resume the entire session
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Takes no parameters.
          Defined in the Python implementation at `deluge/core/core.py:745`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.resume_session"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Empty; this method takes no parameters.
          maxItems: 0
          default: []

    CoreResumeTorrent:
      type: object
      description:
        |-
          Resumes a torrent
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Required parameters, in order: `torrent_id`.
          Defined in the Python implementation at `deluge/core/core.py:759`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.resume_torrent"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 1
          maxItems: 1
          prefixItems:
            - description: "torrent_id (str)"

    CoreResumeTorrents:
      type: object
      description:
        |-
          Resumes a list of torrents
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Optional, in order after those: `torrent_ids` (default `None`).
          Defined in the Python implementation at `deluge/core/core.py:768`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.resume_torrents"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 0
          maxItems: 1
          prefixItems:
            - description: "torrent_ids (List[str]) — optional, defaults to None"

    CoreSetConfig:
      type: object
      description:
        |-
          Set the config with values from dictionary
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Required parameters, in order: `config`.
          Defined in the Python implementation at `deluge/core/core.py:864`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.set_config"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 1
          maxItems: 1
          prefixItems:
            - description: "config (Dict[str, Any])"

    CoreSetSslTorrentCert:
      type: object
      description:
        |-
          Set the SSL certificates used to connect to SSL peers of the given torrent.
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Required parameters, in order: `torrent_id`, `certificate`, `private_key`, `dh_params`.
          Optional, in order after those: `save_to_disk` (default `True`).
          Defined in the Python implementation at `deluge/core/core.py:680`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.set_ssl_torrent_cert"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 4
          maxItems: 5
          prefixItems:
            - description: "torrent_id (str)"
            - description: "certificate (str)"
            - description: "private_key (str)"
            - description: "dh_params (str)"
            - description: "save_to_disk (bool) — optional, defaults to True"

    CoreSetTorrentAutoManaged:
      type: object
      description:
        |-
          Deprecated: Use set_torrent_options with 'auto_managed'
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Required parameters, in order: `torrent_id`, `value`.
          Defined in the Python implementation at `deluge/core/core.py:1012`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.set_torrent_auto_managed"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 2
          maxItems: 2
          prefixItems:
            - description: "torrent_id"
            - description: "value"

    CoreSetTorrentFilePriorities:
      type: object
      description:
        |-
          Deprecated: Use set_torrent_options with 'file_priorities'
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Required parameters, in order: `torrent_id`, `priorities`.
          Defined in the Python implementation at `deluge/core/core.py:1000`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.set_torrent_file_priorities"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 2
          maxItems: 2
          prefixItems:
            - description: "torrent_id"
            - description: "priorities"

    CoreSetTorrentMaxConnections:
      type: object
      description:
        |-
          Deprecated: Use set_torrent_options with 'max_connections'
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Required parameters, in order: `torrent_id`, `value`.
          Defined in the Python implementation at `deluge/core/core.py:976`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.set_torrent_max_connections"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 2
          maxItems: 2
          prefixItems:
            - description: "torrent_id"
            - description: "value"

    CoreSetTorrentMaxDownloadSpeed:
      type: object
      description:
        |-
          Deprecated: Use set_torrent_options with 'max_download_speed'
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Required parameters, in order: `torrent_id`, `value`.
          Defined in the Python implementation at `deluge/core/core.py:994`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.set_torrent_max_download_speed"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 2
          maxItems: 2
          prefixItems:
            - description: "torrent_id"
            - description: "value"

    CoreSetTorrentMaxUploadSlots:
      type: object
      description:
        |-
          Deprecated: Use set_torrent_options with 'max_upload_slots'
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Required parameters, in order: `torrent_id`, `value`.
          Defined in the Python implementation at `deluge/core/core.py:982`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.set_torrent_max_upload_slots"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 2
          maxItems: 2
          prefixItems:
            - description: "torrent_id"
            - description: "value"

    CoreSetTorrentMaxUploadSpeed:
      type: object
      description:
        |-
          Deprecated: Use set_torrent_options with 'max_upload_speed'
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Required parameters, in order: `torrent_id`, `value`.
          Defined in the Python implementation at `deluge/core/core.py:988`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.set_torrent_max_upload_speed"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 2
          maxItems: 2
          prefixItems:
            - description: "torrent_id"
            - description: "value"

    CoreSetTorrentMoveCompleted:
      type: object
      description:
        |-
          Deprecated: Use set_torrent_options with 'move_completed'
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Required parameters, in order: `torrent_id`, `value`.
          Defined in the Python implementation at `deluge/core/core.py:1036`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.set_torrent_move_completed"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 2
          maxItems: 2
          prefixItems:
            - description: "torrent_id"
            - description: "value"

    CoreSetTorrentMoveCompletedPath:
      type: object
      description:
        |-
          Deprecated: Use set_torrent_options with 'move_completed_path'
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Required parameters, in order: `torrent_id`, `value`.
          Defined in the Python implementation at `deluge/core/core.py:1042`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.set_torrent_move_completed_path"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 2
          maxItems: 2
          prefixItems:
            - description: "torrent_id"
            - description: "value"

    CoreSetTorrentOptions:
      type: object
      description:
        |-
          Sets the torrent options for torrent_ids
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Required parameters, in order: `torrent_ids`, `options`.
          Defined in the Python implementation at `deluge/core/core.py:944`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.set_torrent_options"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 2
          maxItems: 2
          prefixItems:
            - description: "torrent_ids (List[str])"
            - description: "options (Dict[str, Any])"

    CoreSetTorrentPrioritizeFirstLast:
      type: object
      description:
        |-
          Deprecated: Use set_torrent_options with 'prioritize_first_last'
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Required parameters, in order: `torrent_id`, `value`.
          Defined in the Python implementation at `deluge/core/core.py:1006`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.set_torrent_prioritize_first_last"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 2
          maxItems: 2
          prefixItems:
            - description: "torrent_id"
            - description: "value"

    CoreSetTorrentRemoveAtRatio:
      type: object
      description:
        |-
          Deprecated: Use set_torrent_options with 'remove_at_ratio'
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Required parameters, in order: `torrent_id`, `value`.
          Defined in the Python implementation at `deluge/core/core.py:1030`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.set_torrent_remove_at_ratio"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 2
          maxItems: 2
          prefixItems:
            - description: "torrent_id"
            - description: "value"

    CoreSetTorrentStopAtRatio:
      type: object
      description:
        |-
          Deprecated: Use set_torrent_options with 'stop_at_ratio'
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Required parameters, in order: `torrent_id`, `value`.
          Defined in the Python implementation at `deluge/core/core.py:1018`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.set_torrent_stop_at_ratio"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 2
          maxItems: 2
          prefixItems:
            - description: "torrent_id"
            - description: "value"

    CoreSetTorrentStopRatio:
      type: object
      description:
        |-
          Deprecated: Use set_torrent_options with 'stop_ratio'
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Required parameters, in order: `torrent_id`, `value`.
          Defined in the Python implementation at `deluge/core/core.py:1024`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.set_torrent_stop_ratio"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 2
          maxItems: 2
          prefixItems:
            - description: "torrent_id"
            - description: "value"

    CoreSetTorrentTrackers:
      type: object
      description:
        |-
          Sets a torrents tracker list. trackers will be ``[{"url", "tier"}]``
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Required parameters, in order: `torrent_id`, `trackers`.
          Defined in the Python implementation at `deluge/core/core.py:964`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.set_torrent_trackers"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 2
          maxItems: 2
          prefixItems:
            - description: "torrent_id (str)"
            - description: "trackers (List[Dict[str, Any]])"

    CoreTestListenPort:
      type: object
      description:
        |-
          Checks if the active port is open
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Takes no parameters.
          Defined in the Python implementation at `deluge/core/core.py:1288`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.test_listen_port"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Empty; this method takes no parameters.
          maxItems: 0
          default: []

    CoreUpdateAccount:
      type: object
      description:
        |-
          Authorisation: admin.
          Forwarded to the daemon over DelugeRPC.
          Required parameters, in order: `username`, `password`, `authlevel`.
          Defined in the Python implementation at `deluge/core/core.py:1365`.
      required: [method]
      properties:
        method:
          type: string
          const: "core.update_account"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 3
          maxItems: 3
          prefixItems:
            - description: "username (str)"
            - description: "password (str)"
            - description: "authlevel (str)"

    DaemonAuthorizedCall:
      type: object
      description:
        |-
          Determines if session auth_level is authorized to call RPC.
          Authorisation: read-only.
          Forwarded to the daemon over DelugeRPC.
          Required parameters, in order: `rpc`.
          Defined in the Python implementation at `deluge/core/daemon.py:173`.
      required: [method]
      properties:
        method:
          type: string
          const: "daemon.authorized_call"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 1
          maxItems: 1
          prefixItems:
            - description: "rpc"

    DaemonGetMethodList:
      type: object
      description:
        |-
          Returns a list of the exported methods.
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Takes no parameters.
          Defined in the Python implementation at `deluge/core/daemon.py:163`.
      required: [method]
      properties:
        method:
          type: string
          const: "daemon.get_method_list"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Empty; this method takes no parameters.
          maxItems: 0
          default: []

    DaemonGetVersion:
      type: object
      description:
        |-
          Returns the daemon version
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Takes no parameters.
          Defined in the Python implementation at `deluge/core/daemon.py:168`.
      required: [method]
      properties:
        method:
          type: string
          const: "daemon.get_version"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Empty; this method takes no parameters.
          maxItems: 0
          default: []

    DaemonShutdown:
      type: object
      description:
        |-
          Authorisation: normal.
          Forwarded to the daemon over DelugeRPC.
          Optional, in order after those: `*args`, `**kwargs`.
          Defined in the Python implementation at `deluge/core/daemon.py:153`.
      required: [method]
      properties:
        method:
          type: string
          const: "daemon.shutdown"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 0
          maxItems: 2
          prefixItems:
            - description: "*args — optional"
            - description: "**kwargs — optional"

    SystemListMethods:
      type: object
      description:
        |-
          Every method this server will answer, its own and the daemon's, sorted.
          Authorisation: none, answered before a session exists.
          Answered by the Web UI server.
          Takes no parameters.
          Defined in the Python implementation at `answered by the JSON handler, not declared with @export`.
      required: [method]
      properties:
        method:
          type: string
          const: "system.listMethods"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Empty; this method takes no parameters.
          maxItems: 0
          default: []

    WebAddHost:
      type: object
      description:
        |-
          Adds a host to the list.
          Authorisation: normal.
          Answered by the Web UI server.
          Required parameters, in order: `host`, `port`.
          Optional, in order after those: `username` (default `''`), `password` (default `''`).
          Defined in the Python implementation at `deluge/ui/web/json_api.py:773`.
      required: [method]
      properties:
        method:
          type: string
          const: "web.add_host"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 2
          maxItems: 4
          prefixItems:
            - description: "host"
            - description: "port"
            - description: "username — optional, defaults to ''"
            - description: "password — optional, defaults to ''"

    WebAddTorrents:
      type: object
      description:
        |-
          Add torrents by file
          Authorisation: normal.
          Answered by the Web UI server.
          Required parameters, in order: `torrents`.
          Defined in the Python implementation at `deluge/ui/web/json_api.py:707`.
      required: [method]
      properties:
        method:
          type: string
          const: "web.add_torrents"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 1
          maxItems: 1
          prefixItems:
            - description: "torrents"

    WebConnect:
      type: object
      description:
        |-
          Connect the web client to a daemon.
          Authorisation: normal.
          Answered by the Web UI server.
          Required parameters, in order: `host_id`.
          Defined in the Python implementation at `deluge/ui/web/json_api.py:452`.
      required: [method]
      properties:
        method:
          type: string
          const: "web.connect"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 1
          maxItems: 1
          prefixItems:
            - description: "host_id"

    WebConnected:
      type: object
      description:
        |-
          The current connection state.
          Authorisation: normal.
          Answered by the Web UI server.
          Takes no parameters.
          Defined in the Python implementation at `deluge/ui/web/json_api.py:467`.
      required: [method]
      properties:
        method:
          type: string
          const: "web.connected"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Empty; this method takes no parameters.
          maxItems: 0
          default: []

    WebDeregisterEventListener:
      type: object
      description:
        |-
          Remove an event listener from the event queue.
          Authorisation: normal.
          Answered by the Web UI server.
          Required parameters, in order: `event`.
          Defined in the Python implementation at `deluge/ui/web/json_api.py:938`.
      required: [method]
      properties:
        method:
          type: string
          const: "web.deregister_event_listener"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 1
          maxItems: 1
          prefixItems:
            - description: "event"

    WebDisconnect:
      type: object
      description:
        |-
          Disconnect the web interface from the connected daemon.
          Authorisation: normal.
          Answered by the Web UI server.
          Takes no parameters.
          Defined in the Python implementation at `deluge/ui/web/json_api.py:477`.
      required: [method]
      properties:
        method:
          type: string
          const: "web.disconnect"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Empty; this method takes no parameters.
          maxItems: 0
          default: []

    WebDownloadTorrentFromUrl:
      type: object
      description:
        |-
          Download a torrent file from a URL to a temporary directory.
          Authorisation: normal.
          Answered by the Web UI server.
          Required parameters, in order: `url`.
          Optional, in order after those: `cookie` (default `None`).
          Defined in the Python implementation at `deluge/ui/web/json_api.py:645`.
      required: [method]
      properties:
        method:
          type: string
          const: "web.download_torrent_from_url"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 1
          maxItems: 2
          prefixItems:
            - description: "url"
            - description: "cookie — optional, defaults to None"

    WebEditHost:
      type: object
      description:
        |-
          Edit host details in the hostlist.
          Authorisation: normal.
          Answered by the Web UI server.
          Required parameters, in order: `host_id`, `host`, `port`.
          Optional, in order after those: `username` (default `''`), `password` (default `''`).
          Defined in the Python implementation at `deluge/ui/web/json_api.py:794`.
      required: [method]
      properties:
        method:
          type: string
          const: "web.edit_host"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 3
          maxItems: 5
          prefixItems:
            - description: "host_id"
            - description: "host"
            - description: "port"
            - description: "username — optional, defaults to ''"
            - description: "password — optional, defaults to ''"

    WebGetConfig:
      type: object
      description:
        |-
          Get the configuration dictionary for the web interface.
          Authorisation: normal.
          Answered by the Web UI server.
          Takes no parameters.
          Defined in the Python implementation at `deluge/ui/web/json_api.py:845`.
      required: [method]
      properties:
        method:
          type: string
          const: "web.get_config"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Empty; this method takes no parameters.
          maxItems: 0
          default: []

    WebGetEvents:
      type: object
      description:
        |-
          Retrieve the pending events for the session.
          Authorisation: normal.
          Answered by the Web UI server.
          Takes no parameters.
          Defined in the Python implementation at `deluge/ui/web/json_api.py:948`.
      required: [method]
      properties:
        method:
          type: string
          const: "web.get_events"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Empty; this method takes no parameters.
          maxItems: 0
          default: []

    WebGetHostStatus:
      type: object
      description:
        |-
          Returns the current status for the specified host.
          Authorisation: normal.
          Answered by the Web UI server.
          Required parameters, in order: `host_id`.
          Defined in the Python implementation at `deluge/ui/web/json_api.py:758`.
      required: [method]
      properties:
        method:
          type: string
          const: "web.get_host_status"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 1
          maxItems: 1
          prefixItems:
            - description: "host_id"

    WebGetHosts:
      type: object
      description:
        |-
          Return the hosts in the hostlist.
          Authorisation: normal.
          Answered by the Web UI server.
          Takes no parameters.
          Defined in the Python implementation at `deluge/ui/web/json_api.py:750`.
      required: [method]
      properties:
        method:
          type: string
          const: "web.get_hosts"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Empty; this method takes no parameters.
          maxItems: 0
          default: []

    WebGetMagnetInfo:
      type: object
      description:
        |-
          Parse a magnet URI for hash and name.
          Authorisation: normal.
          Answered by the Web UI server.
          Required parameters, in order: `uri`.
          Defined in the Python implementation at `deluge/ui/web/json_api.py:702`.
      required: [method]
      properties:
        method:
          type: string
          const: "web.get_magnet_info"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 1
          maxItems: 1
          prefixItems:
            - description: "uri"

    WebGetTorrentFiles:
      type: object
      description:
        |-
          Gets the files for a torrent in tree format
          Authorisation: normal.
          Answered by the Web UI server.
          Required parameters, in order: `torrent_id`.
          Defined in the Python implementation at `deluge/ui/web/json_api.py:630`.
      required: [method]
      properties:
        method:
          type: string
          const: "web.get_torrent_files"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 1
          maxItems: 1
          prefixItems:
            - description: "torrent_id"

    WebGetTorrentInfo:
      type: object
      description:
        |-
          Return information about a torrent on the filesystem.
          Authorisation: normal.
          Answered by the Web UI server.
          Required parameters, in order: `filename`.
          Defined in the Python implementation at `deluge/ui/web/json_api.py:675`.
      required: [method]
      properties:
        method:
          type: string
          const: "web.get_torrent_info"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 1
          maxItems: 1
          prefixItems:
            - description: "filename"

    WebGetTorrentStatus:
      type: object
      description:
        |-
          Get the status for a torrent, filtered by status keys.
          Authorisation: normal.
          Answered by the Web UI server.
          Required parameters, in order: `torrent_id`, `keys`.
          Defined in the Python implementation at `deluge/ui/web/json_api.py:625`.
      required: [method]
      properties:
        method:
          type: string
          const: "web.get_torrent_status"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 2
          maxItems: 2
          prefixItems:
            - description: "torrent_id"
            - description: "keys"

    WebRegisterEventListener:
      type: object
      description:
        |-
          Add a listener to the event queue.
          Authorisation: normal.
          Answered by the Web UI server.
          Required parameters, in order: `event`.
          Defined in the Python implementation at `deluge/ui/web/json_api.py:928`.
      required: [method]
      properties:
        method:
          type: string
          const: "web.register_event_listener"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 1
          maxItems: 1
          prefixItems:
            - description: "event"

    WebRemoveHost:
      type: object
      description:
        |-
          Removes a host from the hostlist.
          Authorisation: normal.
          Answered by the Web UI server.
          Required parameters, in order: `host_id`.
          Defined in the Python implementation at `deluge/ui/web/json_api.py:811`.
      required: [method]
      properties:
        method:
          type: string
          const: "web.remove_host"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 1
          maxItems: 1
          prefixItems:
            - description: "host_id"

    WebSetConfig:
      type: object
      description:
        |-
          Sets the configuration dictionary for the web interface.
          Authorisation: normal.
          Answered by the Web UI server.
          Required parameters, in order: `config`.
          Defined in the Python implementation at `deluge/ui/web/json_api.py:859`.
      required: [method]
      properties:
        method:
          type: string
          const: "web.set_config"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 1
          maxItems: 1
          prefixItems:
            - description: "config"

    WebSetTheme:
      type: object
      description:
        |-
          Sets a new Theme to the WebUI
          Authorisation: normal.
          Answered by the Web UI server.
          Required parameters, in order: `theme`.
          Defined in the Python implementation at `deluge/ui/web/json_api.py:955`.
      required: [method]
      properties:
        method:
          type: string
          const: "web.set_theme"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 1
          maxItems: 1
          prefixItems:
            - description: "theme"

    WebStartDaemon:
      type: object
      description:
        |-
          Starts a local daemon.
          Authorisation: normal.
          Answered by the Web UI server.
          Required parameters, in order: `port`.
          Defined in the Python implementation at `deluge/ui/web/json_api.py:824`.
      required: [method]
      properties:
        method:
          type: string
          const: "web.start_daemon"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 1
          maxItems: 1
          prefixItems:
            - description: "port"

    WebStopDaemon:
      type: object
      description:
        |-
          Authorisation: normal.
          Answered by the Web UI server.
          Required parameters, in order: `host_id`.
          Defined in the Python implementation at `deluge/ui/web/json_api.py:832`.
      required: [method]
      properties:
        method:
          type: string
          const: "web.stop_daemon"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 1
          maxItems: 1
          prefixItems:
            - description: "host_id"

    WebUpdateUi:
      type: object
      description:
        |-
          Gather the information required for updating the web interface.
          Authorisation: normal.
          Answered by the Web UI server.
          Required parameters, in order: `keys`, `filter_dict`.
          Defined in the Python implementation at `deluge/ui/web/json_api.py:490`.
      required: [method]
      properties:
        method:
          type: string
          const: "web.update_ui"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Positional, in the order below.
          minItems: 2
          maxItems: 2
          prefixItems:
            - description: "keys"
            - description: "filter_dict"

    WebutilsGetLanguages:
      type: object
      description:
        |-
          Get the available translated languages
          Authorisation: normal.
          Answered by the Web UI server.
          Takes no parameters.
          Defined in the Python implementation at `deluge/ui/web/json_api.py:974`.
      required: [method]
      properties:
        method:
          type: string
          const: "webutils.get_languages"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Empty; this method takes no parameters.
          maxItems: 0
          default: []

    WebutilsGetThemes:
      type: object
      description:
        |-
          Get the available themes
          Authorisation: normal.
          Answered by the Web UI server.
          Takes no parameters.
          Defined in the Python implementation at `deluge/ui/web/json_api.py:984`.
      required: [method]
      properties:
        method:
          type: string
          const: "webutils.get_themes"
        id:
          $ref: "#/components/schemas/RequestId"
        params:
          type: array
          description: Empty; this method takes no parameters.
          maxItems: 0
          default: []

    CoreAddTorrentFileResult:
      {
        "oneOf": [
          {
            "type": "string"
          },
          {
            "type": "null"
          }
        ],
        "description": "What core.add_torrent_file puts in `result`."
      }

    CoreAddTorrentFileAsyncResult:
      {
        "oneOf": [
          {
            "type": "string"
          },
          {
            "type": "null"
          }
        ],
        "description": "What core.add_torrent_file_async puts in `result`."
      }

    CoreAddTorrentFilesResult:
      {
        "type": "array",
        "description": "What core.add_torrent_files puts in `result`."
      }

    CoreAddTorrentMagnetResult:
      {
        "type": "string",
        "description": "What core.add_torrent_magnet puts in `result`."
      }

    CoreAddTorrentUrlResult:
      {
        "oneOf": [
          {
            "type": "string"
          },
          {
            "type": "null"
          }
        ],
        "description": "What core.add_torrent_url puts in `result`."
      }

    CoreCreateAccountResult:
      {
        "type": "boolean",
        "description": "What core.create_account puts in `result`."
      }

    CoreForceReannounceResult:
      {
        "type": "null",
        "description": "What core.force_reannounce puts in `result`."
      }

    CoreForceRecheckResult:
      {
        "type": "null",
        "description": "What core.force_recheck puts in `result`."
      }

    CoreGetAuthLevelsMappingsResult:
      {
        "type": "array",
        "description": "What core.get_auth_levels_mappings puts in `result`."
      }

    CoreGetCompletionPathsResult:
      {
        "type": "object",
        "description": "What core.get_completion_paths puts in `result`."
      }

    CoreGetConfigResult:
      {
        "type": "object",
        "description": "What core.get_config puts in `result`."
      }

    CoreGetConfigValueResult:
      {
        "description": "What core.get_config_value puts in `result`."
      }

    CoreGetConfigValuesResult:
      {
        "type": "object",
        "description": "What core.get_config_values puts in `result`."
      }

    CoreGetExternalIpResult:
      {
        "type": "string",
        "description": "What core.get_external_ip puts in `result`."
      }

    CoreGetFreeSpaceResult:
      {
        "type": "integer",
        "description": "What core.get_free_space puts in `result`."
      }

    CoreGetKnownAccountsResult:
      {
        "type": "array",
        "items": {
          "type": "object"
        },
        "description": "What core.get_known_accounts puts in `result`."
      }

    CoreGetLibtorrentVersionResult:
      {
        "type": "string",
        "description": "What core.get_libtorrent_version puts in `result`."
      }

    CoreGetListenPortResult:
      {
        "type": "integer",
        "description": "What core.get_listen_port puts in `result`."
      }

    CoreGetMagnetUriResult:
      {
        "type": "string",
        "description": "What core.get_magnet_uri puts in `result`."
      }

    CoreGetProxyResult:
      {
        "type": "object",
        "description": "What core.get_proxy puts in `result`."
      }

    CoreGetSessionStateResult:
      {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "What core.get_session_state puts in `result`."
      }

    CoreGetSessionStatusResult:
      {
        "type": "object",
        "description": "What core.get_session_status puts in `result`."
      }

    CoreGetSslListenPortResult:
      {
        "type": "integer",
        "description": "What core.get_ssl_listen_port puts in `result`."
      }

    CoreGetTorrentStatusResult:
      {
        "type": "object",
        "description": "What core.get_torrent_status puts in `result`."
      }

    CoreGetTorrentsStatusResult:
      {
        "type": "object",
        "description": "What core.get_torrents_status puts in `result`."
      }

    CoreGlobResult:
      {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "What core.glob puts in `result`."
      }

    CoreIsSessionPausedResult:
      {
        "type": "boolean",
        "description": "What core.is_session_paused puts in `result`."
      }

    CorePauseSessionResult:
      {
        "type": "null",
        "description": "What core.pause_session puts in `result`."
      }

    CorePauseTorrentResult:
      {
        "type": "null",
        "description": "What core.pause_torrent puts in `result`."
      }

    CorePauseTorrentsResult:
      {
        "type": "null",
        "description": "What core.pause_torrents puts in `result`."
      }

    CorePrefetchMagnetMetadataResult:
      {
        "type": "array",
        "description": "What core.prefetch_magnet_metadata puts in `result`."
      }

    CoreQueueBottomResult:
      {
        "type": "null",
        "description": "What core.queue_bottom puts in `result`."
      }

    CoreQueueDownResult:
      {
        "type": "null",
        "description": "What core.queue_down puts in `result`."
      }

    CoreQueueTopResult:
      {
        "type": "null",
        "description": "What core.queue_top puts in `result`."
      }

    CoreQueueUpResult:
      {
        "type": "null",
        "description": "What core.queue_up puts in `result`."
      }

    CoreRemoveAccountResult:
      {
        "type": "boolean",
        "description": "What core.remove_account puts in `result`."
      }

    CoreRemoveTorrentResult:
      {
        "type": "boolean",
        "description": "What core.remove_torrent puts in `result`."
      }

    CoreRemoveTorrentsResult:
      {
        "type": "array",
        "items": {
          "type": "array"
        },
        "description": "What core.remove_torrents puts in `result`."
      }

    CoreResumeSessionResult:
      {
        "type": "null",
        "description": "What core.resume_session puts in `result`."
      }

    CoreResumeTorrentResult:
      {
        "type": "null",
        "description": "What core.resume_torrent puts in `result`."
      }

    CoreResumeTorrentsResult:
      {
        "type": "null",
        "description": "What core.resume_torrents puts in `result`."
      }

    CoreSetTorrentOptionsResult:
      {
        "type": "null",
        "description": "What core.set_torrent_options puts in `result`."
      }

    CoreSetTorrentTrackersResult:
      {
        "type": "null",
        "description": "What core.set_torrent_trackers puts in `result`."
      }

    CoreTestListenPortResult:
      {
        "oneOf": [
          {
            "type": "boolean"
          },
          {
            "type": "null"
          }
        ],
        "description": "What core.test_listen_port puts in `result`."
      }

    CoreUpdateAccountResult:
      {
        "type": "boolean",
        "description": "What core.update_account puts in `result`."
      }


# Methods the Python implementation had and redeluge does not.
# They managed plugins, and there is no plugin system:
#   `core.disable_plugin`, `core.enable_plugin`, `core.get_available_plugins`, `core.get_enabled_plugins`, `core.rescan_plugins`, `core.upload_plugin`, `web.get_plugin_info`, `web.get_plugin_resources`, `web.get_plugins`, `web.upload_plugin`
