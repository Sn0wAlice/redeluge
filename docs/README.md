# docs

One file: `openapi.yaml`, the API specification, generated from
`contract/rpc-api.json` by `tools/gen_openapi.py`. The gate fails if it is out
of date.

The prose documentation lives in `wiki/` at the repository root, which is
mirrored to <https://github.com/Sn0wAlice/redeluge/wiki>.
